export enum TabNames {
  tabs = 'Tabs',
  history = 'History',
  newWorkout = 'newWorkout',
  newWorkoutForTabIcon = 'New',
  timer = 'Timer'
}

export enum workoutTypeTabs {
  straight = 'Straight',
  superset = 'superset',
  interval = 'interval',
}