export enum TabNames {
  tabs = 'Tabs',
  history = 'History',
  newWorkout = 'newWorkout',
  newWorkoutForTabIcon = 'New',
  timer = 'Timer'
}

export enum WorkoutTabsType {
  straight = 'Straight',
  superset = 'superset',
  interval = 'interval',
}